{
  "name": "eliancpastor-sites",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo of sites served at eliancpastor.github.io — portfolio hub at the root, each project mounted at a sub-path.",
  "scripts": {
    "dev:portfolio": "npm --prefix sites/portfolio run dev",
    "dev:resole": "npm --prefix sites/resole run dev",
    "build:portfolio": "npm --prefix sites/portfolio run build",
    "build:resole": "npm --prefix sites/resole run build",
    "build": "npm run build:portfolio && npm run build:resole && npm run assemble",
    "assemble": "rm -rf dist && mkdir -p dist/resole && cp -R sites/portfolio/dist/. dist/ && cp -R sites/resole/dist/. dist/resole/",
    "deploy": "npx gh-pages -d dist -m 'deploy: portfolio + sites'"
  }
}
